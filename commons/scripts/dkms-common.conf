# Shared DKMS settings for consumer repositories.

set -e

MODULE_NAME="$(tr -d '[:space:]' < MODULE_NAME)"

PACKAGE_NAME="$MODULE_NAME"
BUILT_MODULE_NAME[0]="$MODULE_NAME"

PACKAGE_VERSION="$(cat VERSION)"

BUILT_MODULE_LOCATION[0]="."
DEST_MODULE_LOCATION[0]="/updates/dkms"
AUTOINSTALL="yes"

MAKE[0]="make KERNELRELEASE=${kernelver}"
CLEAN="make clean KERNELRELEASE=${kernelver}"

POST_INSTALL="./commons/scripts/dkms-post-install.sh"
PRE_REMOVE="./commons/scripts/dkms-pre-remove.sh"
